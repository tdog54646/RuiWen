<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.qa.mapper.QaMessageMapper">

    <resultMap id="QaMessageResultMap" type="com.tongji.qa.model.QaMessage">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="userId" column="user_id"/>
        <result property="role" column="role"/>
        <result property="content" column="content"/>
        <result property="imageUrls" column="image_urls"
                typeHandler="com.tongji.qa.mapper.JsonStringListTypeHandler"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.tongji.qa.model.QaMessage">
        INSERT INTO qa_messages (id, conversation_id, user_id, role, content, image_urls, status, created_at)
        VALUES (#{id}, #{conversationId}, #{userId}, #{role}, #{content},
                #{imageUrls, typeHandler=com.tongji.qa.mapper.JsonStringListTypeHandler},
                #{status}, #{createdAt})
    </insert>

    <select id="findById" resultMap="QaMessageResultMap">
        SELECT * FROM qa_messages WHERE id = #{id}
    </select>

    <select id="listRecentByConversation" resultMap="QaMessageResultMap">
        SELECT * FROM qa_messages
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="listByConversation" resultMap="QaMessageResultMap">
        SELECT * FROM qa_messages
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="listRecentByUser" resultMap="QaMessageResultMap">
        SELECT * FROM qa_messages
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
